apiVersion: apps/v1
kind: Deployment
metadata:
  name: rating-service
  namespace: homefood
  labels:
    app: rating-service
    version: "1.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rating-service
  template:
    metadata:
      labels:
        app: rating-service
    spec:
      containers:
        - name: rating-service
          image: ghcr.io/syskantechnosoft2025/homefood-rating-service:latest
          ports:
            - containerPort: 8086
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8086
            initialDelaySeconds: 60
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8086
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: rating-service
  namespace: homefood
spec:
  selector:
    app: rating-service
  ports:
    - port: 8086
      targetPort: 8086
  type: ClusterIP